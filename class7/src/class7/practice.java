package class7;

public class practice {
	int[] hist = new int[5];
	Scanner in = new Scanner(System.in);
	
	public static void main(String[] args) {
		int inputValue = max(i, j, k, l, m);
	}
	
	public static int max(int[??]) {
		int result;
		
		if (inputValue >=90)
			result= ++hist[0];
		else if (inputValue >=80)
			result= ++hist[1];
		else if (inputValue >=70)
			result= ++hist[2];
		else if (inputValue >=60)
			result= ++hist[3];
		else if (inputValue >=0)
			result= ++hist[4];
		else System.out.println("End of input.");
		
		return result; 
		
	}
}
